import 'base_dialog_form.dart';
import 'package:flutter/material.dart';

class InformationDialogForm extends StatelessWidget {
  const InformationDialogForm({super.key});

  @override
  Widget build(BuildContext context) {
    return BaseDialogForm(child: Container());
  }
}

import 'base_dialog_form.dart';
import 'package:flutter/material.dart';

class ConfirmDialogForm extends StatelessWidget {
  const ConfirmDialogForm({super.key});

  @override
  Widget build(BuildContext context) {
    return BaseDialogForm(child: Container());
  }
}
